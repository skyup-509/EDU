# 최단 경로 알고리즘 (Graph Shortest Path Algorithm)
> References: 
>
> 1. [Velog: Graph Shortest Path](https://velog.io/@dankj1991/Algorithms-Graph-Shortest-Path)
>
> 2. [Velog: Dijkstra Algorithm](https://velog.io/@hyukwon/%EB%8B%A4%EC%9D%B5%EC%8A%A4%ED%8A%B8%EB%9D%BC-%EC%95%8C%EA%B3%A0%EB%A6%AC%EC%A6%98Dijkstra-Algorithm)
## Dijkstra
	- 특정 시작점에서 다른 모든 정점으로의 최단 경로 탐색
	- 음수 가중치가 있는 그래프에서는 사용 불가
	- 네비게이션 시스템에 사용

![다익스트라 알고리즘 로직 시각화](https://velog.velcdn.com/images/hyukwon/post/9c3c6028-d07b-49e7-831f-5a002f0591b7/image.gif)


### 예시 코드
```python
import heapq

def dijkstra(start, graph):
    # 최단 거리 저장 딕셔너리 초기화
    distances = {node: float('inf') for node in graph}
    distances[start] = 0
    pq = [(0, start)]	# 우선순위 큐 초기화 (거리, 노드)

    while pq:
        current_distance, current_node = heapq.heappop(pq)

        # 이미 처리된 노드라면 스킵
        if current_distance > distances[current_node]:
            continue

        # 인접 노드들의 거리 갱신
        for neighbor, weight in graph[current_node]:
            distance = current_distance + weight
            
            # 최단 거리 갱신 시 우선순위 큐에 추가
            if distance < distances[neighbor]:
                distances[neighbor] = distance
                heapq.heappush(pq, (distance, neighbor))

    return distances

# 그래프 정의
graph = {
    'A': [('B', 7), ('C', 9), ('F', 14)],
    'B': [('A', 7), ('C', 10), ('D', 15)],
    'C': [('A', 9), ('B', 10), ('D', 11), ('F', 2)],
    'D': [('B', 15), ('C', 11), ('E', 6)],
    'E': [('D', 6), ('F', 9)],
    'F': [('A', 14), ('C', 2), ('E', 9)]
}

# 다익스트라 알고리즘 수행
start_node = 'A'
distances = dijkstra(start_node, graph)

# 최단 거리 출력
print("Shortest distances from node A:")
for node in sorted(distances):
    print(f"{node}: {distances[node]}")
```
## BEllman-Ford
	- Dijkstra와 동일하게 특정 시작점에서 다른 모든 정점으로의 최단 경로 탐색
	- Dijkstra와 다르게 음수 가중치가 있는 그래프에서도 사용 가능
	- 음수 사이클을 탐지하는 추가 과정 필요
	- 금융 거래 네트워크, 차익 거래 탐색
## Floyd-Warshall
	- 모든 정점 쌍 간의 최단 경로 탐색, DP 사용
	- 음수 사이클의 경우, 정확도가 낮음
	- 네트워크 최적화, 사회적 거리 최적화 (감염병 확산 방지 및 사회적 손실 최적화)